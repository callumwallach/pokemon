# Redis connection string (default: local Redis on standard port)
REDIS_URL=redis://localhost:6379

# Cache TTL in milliseconds (default: 1 hour)
CACHE_TTL_MS=3600000

# Server port (default: 3000)
PORT=3000
